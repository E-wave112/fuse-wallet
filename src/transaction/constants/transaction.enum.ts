export enum TransactionType {
    DEBIT = 'DEBIT',
    CREDIT = 'CREDIT',
    PEER_TRANSFER = 'PEER-TRANSFER',
}
export enum TransactionStatus {
    PENDING = 'PENDING',
    SUCCESS = 'SUCCESS',
    FAILED = 'FAILED',
}
